version: 1
applications:
  - appRoot: frontend
    frontend:
      phases:
        preBuild:
          commands:
            - echo "Installing dependencies..."
            - npm ci
        build:
          commands:
            - echo "Building React app..."
            - echo "Building React app with API URLs configured..."
            - npm run build
      artifacts:
        baseDirectory: dist
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
    test:
      phases:
        preTest:
          commands:
            - echo "Installing test dependencies..."
            - npm ci
        test:
          commands:
            - echo "Running unit tests..."
            - npm run test:run
      artifacts:
        baseDirectory: coverage
        files:
          - '**/*'