version: 1
applications:
  - appRoot: frontend
    frontend:
      phases:
        preBuild:
          commands:
            - echo "Node version:" && node --version
            - echo "Current directory:" && pwd
            - echo "Checking for package.json..."
            - ls -la package.json || echo "No package.json found"
            - echo "Checking for package-lock.json..."
            - ls -la package-lock.json || echo "No package-lock.json found"
            - echo "NPM config:"
            - npm config list
            - echo "Installing dependencies..."
            - npm install --no-save --prefer-offline --no-audit --no-fund --loglevel verbose
        build:
          commands:
            - echo "Building React app..."
            - npm run build
            - echo "Build complete, checking dist directory..."
            - ls -la dist/
      artifacts:
        baseDirectory: dist
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*